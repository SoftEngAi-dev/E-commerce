import type{NormalizedProduct}from"../domain/providers.js";
export interface CatalogStore{upsert(product:NormalizedProduct):Promise<void>;get(source:string,externalId:string):Promise<NormalizedProduct|undefined>;list(limit?:number):Promise<NormalizedProduct[]>}
export class InMemoryCatalog implements CatalogStore{private items=new Map<string,NormalizedProduct>();private key(p:NormalizedProduct){return p.source+"::"+p.externalId}async upsert(p:NormalizedProduct){if(p.stock<0||p.cost<0||!p.title.trim())throw new Error("Invalid normalized product");this.items.set(this.key(p),{...p})}async get(source:string,externalId:string){const p=this.items.get(source+"::"+externalId);return p?{...p}:undefined}async list(limit=100){return[...this.items.values()].slice(0,limit).map(p=>({...p}))}}
