import test from "node:test";import assert from "node:assert/strict";
import{campaignRisk,evaluateAgentAction,isCategoryAllowed,isChannelAllowedForMarket,scoreProduct,checkCompliance,assessMarket}from"../src/index.js";

test("campaign budget risk escalates",()=>{assert.equal(campaignRisk({name:"Pilot",market:"UY",channel:"social",budget:100,currency:"UYU",policy:{}}).risk,"low");assert.equal(campaignRisk({name:"Scale",market:"UY",channel:"social",budget:2000,currency:"UYU",policy:{}}).risk,"high")});
test("agent action policy blocks dangerous operations",()=>{assert.equal(evaluateAgentAction({allowLowMedium:true,requireEvidence:true,maxBudget:100,blockedActions:["refund.execute"]},{action:"refund.execute",risk:"low",evidence:["order"]}).allowed,false);assert.equal(evaluateAgentAction({allowLowMedium:true,requireEvidence:true,maxBudget:100,blockedActions:[]},{action:"catalog.enrich",risk:"medium",evidence:["supplier-feed"]}).allowed,true)});
test("market policy gates channels and categories",()=>{const p={market:"UY",currency:"UYU",enabled:true,allowedChannels:["store"],blockedCategories:["regulated"]};assert.equal(isChannelAllowedForMarket(p,"store"),true);assert.equal(isChannelAllowedForMarket(p,"marketplace"),false);assert.equal(isCategoryAllowed(p,"regulated"),false)});
test("intelligence and compliance remain deterministic",()=>{const s=scoreProduct({demand:80,margin:85,competition:30,supplier:90,shipping:20,risk:15,trend:75});assert.ok(s.score>75);const c=checkCompliance({policy:{dropshippingAllowed:true,internationalSalesAllowed:true},market:"UY",claims:[],channel:"store"});assert.equal(c.allowed,true);const m=assessMarket({demand:80,competition:35,shipping:30,paymentCoverage:85,regulatoryComplexity:25,supplierCoverage:80});assert.equal(m.status,"pilot")});
