import test from "node:test";import assert from "node:assert/strict";import{InMemoryJobQueue,exponentialBackoff}from"../src/application/job-queue.js";
test("queue retries and dead-letters",async()=>{const q=new InMemoryJobQueue();await q.enqueue({id:"j1",type:"sync",payload:{},status:"queued",attempts:0,maxAttempts:2,availableAt:0});const a=await q.claim(0);assert.equal(a?.attempts,1);assert.equal(await q.fail("j1","network",0),"queued");const b=await q.claim(0);assert.equal(b?.attempts,2);assert.equal(await q.fail("j1","network",0),"dead");assert.equal(exponentialBackoff(3),4000)});
