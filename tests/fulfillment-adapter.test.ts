import test from"node:test";import assert from"node:assert/strict";import{HttpFulfillmentProvider}from"../src/adapters/http-fulfillment.js";
test("generic fulfillment adapter sends idempotent request",async()=>{const original=globalThis.fetch;globalThis.fetch=(async(input,init)=>{assert.equal(String(input),"https://fulfill.test/fulfillments");const headers=new Headers(init?.headers);assert.equal(headers.get("x-idempotency-key"),"fulfill-1");return new Response(JSON.stringify({id:"f-1"}),{status:201})}) as typeof fetch;try{const p=new HttpFulfillmentProvider({baseUrl:"https://fulfill.test",token:"secret"});const r=await p.submit({orderId:"o1",items:[{sku:"A",quantity:2}],address:"{}",idempotencyKey:"fulfill-1"});assert.equal(r.externalId,"f-1")}finally{globalThis.fetch=original}});