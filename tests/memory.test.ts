import test from"node:test";import assert from"node:assert/strict";import{OpenAICompatibleAIProvider}from"../src/ai/provider.js";
test("AI provider remains provider-neutral and configurable",async()=>{const original=globalThis.fetch;globalThis.fetch=(async(_input,init)=>{const body=JSON.parse(String(init?.body));assert.equal(body.model,"local-model");return new Response(JSON.stringify({model:"local-model",choices:[{message:{content:"ok"}}],usage:{prompt_tokens:4,completion_tokens:2}}),{status:200})}) as typeof fetch;try{const p=new OpenAICompatibleAIProvider({id:"local",baseUrl:"http://localhost:11434",model:"local-model"});const r=await p.complete({system:"system",prompt:"hello"});assert.equal(r.output,"ok");assert.equal(r.usage?.outputTokens,2)}finally{globalThis.fetch=original}});
